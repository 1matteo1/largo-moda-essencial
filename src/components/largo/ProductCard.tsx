import { Link } from "@tanstack/react-router";
import { formatBRL, type Product } from "@/data/products";
export function ProductCard({product}:{product:Product}){const off=product.oldPrice?Math.round((1-product.price/product.oldPrice)*100):0;return <article className="group min-w-0"><Link to="/produto/$slug" params={{slug:product.slug}} className="block"><div className="relative aspect-[4/5] overflow-hidden bg-muted"><img src={product.image} alt={product.name} width={1024} height={1280} loading="lazy" className="h-full w-full object-cover transition-transform duration-700 group-hover:scale-[1.025]"/>{off>0&&<span className="absolute left-3 top-3 bg-accent px-2 py-1 text-[11px] font-semibold text-accent-foreground">-{off}%</span>}{product.new&&<span className="absolute right-3 top-3 bg-primary px-2 py-1 text-[11px] uppercase text-primary-foreground">Novo</span>}</div><div className="pt-3"><p className="text-[11px] uppercase text-muted-foreground">{product.audience} · {product.category}</p><h3 className="mt-1 font-medium text-foreground">{product.name}</h3><div className="mt-1 flex flex-wrap items-center gap-2 text-sm"><span>{formatBRL(product.price)}</span>{product.oldPrice&&<span className="text-muted-foreground line-through">{formatBRL(product.oldPrice)}</span>}</div><p className="mt-1 text-xs text-muted-foreground">ou 3x de {formatBRL(product.price/3)} sem juros</p></div></Link></article>}
